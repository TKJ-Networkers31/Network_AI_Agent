/**
 * theme/spacing.js — layout scale.
 * Desktop content is capped, never full-stretch (spec: max 1600px
 * shell, 1100px content column).
 */

export const spacing = {
  shellMaxWidth: "1600px",
  contentMaxWidth: "1100px",
  sidebarWidth: "17rem",   // 272px, floating
  gutter: "1.5rem",
};

export default spacing;
